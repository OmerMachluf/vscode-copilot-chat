# VS Code Copilot Web Gateway - Production Environment Configuration
# Copy this file to /etc/copilot-gateway/gateway.env and configure

# =============================================================================
# REQUIRED SETTINGS - Must be configured before starting
# =============================================================================

# JWT secret for token signing (REQUIRED in production)
# Generate with: openssl rand -base64 64
JWT_SECRET=

# =============================================================================
# SERVER SETTINGS
# =============================================================================

# Server port (default: 3000)
PORT=3000

# Node environment (production, development, test)
NODE_ENV=production

# JWT token expiration time (default: 24h)
# Examples: 1h, 12h, 24h, 7d
JWT_EXPIRES_IN=24h

# =============================================================================
# VS CODE EXTENSION API
# =============================================================================

# URL of the VS Code extension's HTTP API
# This should point to the machine running VS Code with the Copilot extension
EXTENSION_API_URL=http://localhost:3001

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# CORS allowed origins (comma-separated, leave empty for same-origin only)
# Example: https://example.com,https://app.example.com
CORS_ORIGINS=

# Rate limiting window in milliseconds (default: 900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per rate limit window (default: 100)
RATE_LIMIT_MAX=100

# =============================================================================
# LOGGING
# =============================================================================

# Enable request logging (default: false in production)
ENABLE_LOGGING=false
