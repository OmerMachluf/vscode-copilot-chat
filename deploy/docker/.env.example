# =============================================================================
# VS Code Copilot Web Gateway - Docker Environment Configuration
# =============================================================================
#
# Copy this file to .env and configure before running docker-compose.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your settings
#   docker-compose up -d
#
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED SETTINGS
# -----------------------------------------------------------------------------

# JWT secret for token signing (REQUIRED)
# Generate with: openssl rand -base64 64
JWT_SECRET=

# -----------------------------------------------------------------------------
# SERVER SETTINGS
# -----------------------------------------------------------------------------

# Port to expose the gateway on the host (default: 3000)
GATEWAY_PORT=3000

# Node environment (production, development)
NODE_ENV=production

# JWT token expiration time (default: 24h)
JWT_EXPIRES_IN=24h

# -----------------------------------------------------------------------------
# VS CODE EXTENSION API
# -----------------------------------------------------------------------------

# URL of the VS Code extension's HTTP API
# Use host.docker.internal to access services on the Docker host
# Examples:
#   - Local VS Code: http://host.docker.internal:3001
#   - Remote server: http://192.168.1.100:3001
EXTENSION_API_URL=http://host.docker.internal:3001

# -----------------------------------------------------------------------------
# SECURITY SETTINGS
# -----------------------------------------------------------------------------

# CORS allowed origins (comma-separated, leave empty for same-origin only)
# Example: https://example.com,https://app.example.com
CORS_ORIGINS=

# Rate limiting window in milliseconds (default: 900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per rate limit window (default: 100)
RATE_LIMIT_MAX=100

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------

# Enable request logging (default: false in production)
# Set to true for debugging
ENABLE_LOGGING=false

# -----------------------------------------------------------------------------
# SSL CONFIGURATION (for nginx profile)
# -----------------------------------------------------------------------------

# Domain name for SSL certificate
# DOMAIN=your-domain.com

# Email for Let's Encrypt notifications
# CERTBOT_EMAIL=admin@your-domain.com
